load("@rules_java//java:defs.bzl", "java_binary", "java_library", "java_test")
load("//:config.bzl", "version")
load("//tools/build:client_test.bzl", "client_test")
load("//tools/build:java.bzl", "java_checkstyle_test")
load("//tools/krpctools:clientgen.bzl", "clientgen_java")

exports_files(["CHANGELOG.md"])

copts = [
    "-Werror",
    "-Xlint:all",
]

filegroup(
    name = "java",
    srcs = [":krpc-jar"],
    visibility = ["//visibility:public"],
)

genrule(
    name = "krpc-jar",
    srcs = [":krpc"],
    outs = ["krpc-java-%s.jar" % version],
    cmd = 'cp "$<" "$@"',
    output_to_bindir = True,
    visibility = ["//visibility:public"],
)

java_library(
    name = "krpc",
    srcs = glob(["src/krpc/**/*.java"]) + [
        ":services-debug",
        ":services-dockingcamera",
        ":services-drawing",
        ":services-infernalrobotics",
        ":services-kerbalalarmclock",
        ":services-krpc",
        ":services-lidar",
        ":services-remotetech",
        ":services-spacecenter",
        ":services-stdlib",
        ":services-ui",
        "//protobuf:java",
    ],
    javacopts = copts,
    resources = [
        "CHANGELOG.md",
        "LICENSE",
        "README.txt",
        "//:COPYING",
        "//:COPYING.LESSER",
        "//:version",
    ],
    visibility = ["//visibility:public"],
    deps = [
        "@maven//:com_google_protobuf_protobuf_java",
        "@maven//:org_javatuples_javatuples",
    ],
)

java_library(
    name = "testlib",
    srcs = glob(["test/**/*.java"]) + [":services-testservice"],
    javacopts = copts,
    deps = [
        ":krpc",
        "@maven//:com_google_protobuf_protobuf_java",
        "@maven//:junit_junit",
        "@maven//:org_hamcrest_hamcrest",
        "@maven//:org_javatuples_javatuples",
    ],
)

test_runtime_deps = [
    ":krpc",
    ":testlib",
    "@maven//:com_google_protobuf_protobuf_java",
    "@maven//:junit_junit",
    "@maven//:org_hamcrest_hamcrest",
    "@maven//:org_javatuples_javatuples",
]

# What the client does regardless of the transport it talks over: encoding and decoding the
# values a call carries. It needs no server, so it runs on its own rather than under the
# harness that starts one.
java_test(
    name = "unit",
    size = "small",
    javacopts = copts,
    main_class = "krpc.client.UnitTestSuite",
    runtime_deps = test_runtime_deps,
)

# The transport, one test each. Both run against a server the test listens on itself, which
# answers the handshake and every request after it, so neither needs a kRPC server.
java_test(
    name = "communication-tcpip",
    size = "small",
    javacopts = copts,
    main_class = "krpc.client.TcpIpConnectionTestSuite",
    tags = ["requires-network"],
    runtime_deps = test_runtime_deps,
)

java_test(
    name = "communication-localsocket",
    size = "small",
    javacopts = copts,
    main_class = "krpc.client.LocalSocketConnectionTestSuite",
    runtime_deps = test_runtime_deps,
)

# The tests that talk to a server, run end to end against a TestServer by the targets below
java_test(
    name = "testexe",
    size = "medium",
    javacopts = copts,
    main_class = "krpc.client.TestSuite",
    tags = ["requires-network"],
    runtime_deps = test_runtime_deps,
)

# The benchmark program, run by //tools/benchmarks:java. Not a test: it measures.
java_binary(
    name = "benchmark",
    srcs = ["benchmark/krpc/client/Benchmark.java"] + [":services-testservice"],
    javacopts = copts,
    main_class = "krpc.client.Benchmark",
    visibility = ["//tools/benchmarks:__pkg__"],
    deps = [
        ":krpc",
        "@maven//:com_google_protobuf_protobuf_java",
        "@maven//:org_javatuples_javatuples",
    ],
)

test_suite(
    name = "test",
    tests = [
        ":client-localsocket",
        ":client-tcpip",
        ":communication-localsocket",
        ":communication-tcpip",
        ":lint",
        ":unit",
    ],
)

# The client end to end against a TestServer, once per transport it can reach one over.
client_test(
    name = "client-tcpip",
    size = "medium",
    server_executable = "//tools/TestServer",
    tags = ["requires-network"],
    test_executable = ":testexe",
    visibility = ["//:__pkg__"],
)

client_test(
    name = "client-localsocket",
    size = "medium",
    server_executable = "//tools/TestServer",
    server_type = "localsocket",
    test_executable = ":testexe",
    visibility = ["//:__pkg__"],
)

java_checkstyle_test(
    name = "lint",
    size = "small",
    srcs = glob(["**/*.java"]),
)

clientgen_java(
    name = "services-krpc",
    out = "services/KRPC.java",
    defs = "//core:ServiceDefinitions",
    service = "KRPC",
)

clientgen_java(
    name = "services-spacecenter",
    out = "services/SpaceCenter.java",
    defs = "//service/SpaceCenter:ServiceDefinitions",
    service = "SpaceCenter",
)

clientgen_java(
    name = "services-drawing",
    out = "services/Drawing.java",
    defs = "//service/Drawing:ServiceDefinitions",
    service = "Drawing",
)

clientgen_java(
    name = "services-infernalrobotics",
    out = "services/InfernalRobotics.java",
    defs = "//service/InfernalRobotics:ServiceDefinitions",
    service = "InfernalRobotics",
)

clientgen_java(
    name = "services-kerbalalarmclock",
    out = "services/KerbalAlarmClock.java",
    defs = "//service/KerbalAlarmClock:ServiceDefinitions",
    service = "KerbalAlarmClock",
)

clientgen_java(
    name = "services-remotetech",
    out = "services/RemoteTech.java",
    defs = "//service/RemoteTech:ServiceDefinitions",
    service = "RemoteTech",
)

clientgen_java(
    name = "services-ui",
    out = "services/UI.java",
    defs = "//service/UI:ServiceDefinitions",
    service = "UI",
)

clientgen_java(
    name = "services-stdlib",
    out = "services/StdLib.java",
    defs = "//core:StdLibServiceDefinitions",
    service = "StdLib",
)

clientgen_java(
    name = "services-lidar",
    out = "services/LiDAR.java",
    defs = "//service/LiDAR:ServiceDefinitions",
    service = "LiDAR",
)

clientgen_java(
    name = "services-dockingcamera",
    out = "services/DockingCamera.java",
    defs = "//service/DockingCamera:ServiceDefinitions",
    service = "DockingCamera",
)

clientgen_java(
    name = "services-debug",
    out = "services/Debug.java",
    defs = "//service/Debug:ServiceDefinitions",
    service = "Debug",
)

clientgen_java(
    name = "services-testservice",
    out = "services/TestService.java",
    defs = "//tools/TestServer:ServiceDefinitions",
    service = "TestService",
)
