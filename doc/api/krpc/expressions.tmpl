.. default-domain:: {{ domain.sphinxname }}
.. highlight:: {{ domain.highlight }}
{{ domain.currentmodule('KRPC') }}
{% import domain.macros as macros with context %}

Expressions
===========

The nodes a server side function is built from, and the types they work with. See the
:rst:ref:`server side functions tutorial <tutorial-server-side-functions>` for how a function is
assembled and run.

{{ macros.class(services['KRPC'].classes['Expression']) }}
{{ macros.class(services['KRPC'].classes['Type']) }}
{{ macros.enumeration(services['KRPC'].enumerations['TypeCode']) }}
